<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.neo4j.tools</groupId>
  <artifactId>neo4j-cert-tool</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>neo4j-cert-tool</name>
  <description>Generates TLS certificates for Neo4j 2025.x clusters using only the JDK</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!--
      The tool targets JDK 25 LTS as its minimum runtime. Building with a newer JDK is
      fine: the release setting makes javac compile against the JDK 25 API signatures,
      so the resulting class files run on any JDK 25 or later.
    -->
    <maven.compiler.release>25</maven.compiler.release>
    <junit.version>5.11.4</junit.version>
    <surefire.version>3.5.2</surefire.version>
  </properties>

  <dependencies>
    <!--
      The shipped artifact has zero runtime dependencies: everything the tool does
      (DER/X.509 encoding, key generation, PKCS#8 encryption, POSIX/ACL permissions)
      is implemented against the JDK. JUnit is test scope only and is never packaged.
    -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>neo4j-cert-tool</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <compilerArgs>
            <arg>-Xlint:all</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <!--
            The runnable jar lands in bin/ rather than target/, so the built tool sits in a
            predictable place. Intermediate build output stays in target/, and bin/ is
            gitignored because its only contents are generated.
          -->
          <outputDirectory>${project.basedir}/bin</outputDirectory>
          <archive>
            <manifest>
              <mainClass>com.neo4j.tools.certtool.Main</mainClass>
            </manifest>
            <manifestEntries>
              <Implementation-Version>${project.version}</Implementation-Version>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-clean-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <!--
            'mvn clean' only knows about target/, so the jar in bin/ has to be listed
            explicitly or a stale build would survive a clean. Restricted to *.jar so
            nothing else that ends up in bin/ is removed.
          -->
          <filesets>
            <fileset>
              <directory>${project.basedir}/bin</directory>
              <includes>
                <include>*.jar</include>
              </includes>
            </fileset>
          </filesets>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>enforce-no-runtime-deps</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <!-- Fail the build if anyone ever adds a compile/runtime dependency. -->
                <bannedDependencies>
                  <searchTransitive>true</searchTransitive>
                  <excludes>
                    <exclude>*:*:*:*:compile</exclude>
                    <exclude>*:*:*:*:runtime</exclude>
                    <exclude>*:*:*:*:provided</exclude>
                  </excludes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
